# PMI 저장소 루트 통합 가상환경 (.venv)
#   cd /path/to/PMI && python3 -m venv .venv && ./.venv/bin/pip install -U pip && ./.venv/bin/pip install -r requirements.txt
#
# 포함: rl_pmi, mujoco_pmi_viz, analysis/python 등 하위 스크립트 공통 의존성
# 하위 폴더의 requirements.txt는 최소·참고용으로 유지합니다.

mujoco>=3.1.0
numpy>=1.20.0
PyYAML>=6.0
matplotlib>=3.5.0
gymnasium>=0.29.0
stable-baselines3>=2.2.0
torch>=2.0.0
tensorboard>=2.14.0
moviepy>=1.0.3
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
