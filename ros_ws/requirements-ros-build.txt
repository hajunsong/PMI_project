# ROS 2 colcon / ament가 package.xml 처리 시 사용하는 파서 의존성.
# 프로젝트 루트에서 ``source .venv/bin/activate`` 한 뒤 빌드하면,
# CMake가 이 venv의 Python으로 package_xml_2_cmake.py 를 실행하므로 여기 포함된 패키지가 필요합니다.
catkin_pkg>=1.0.0
